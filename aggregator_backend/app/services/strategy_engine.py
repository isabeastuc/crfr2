from typing import Dict, List

def select_best_strategy(protocol_apy: Dict[str, float]) -> str:
    if not protocol_apy:
        return ""
    return max(protocol_apy, key=protocol_apy.get)
# core enhancement 1: strategy selector tuning

# core enhancement 4: strategy selector tuning

# core enhancement 7: strategy selector tuning

# core enhancement 10: strategy selector tuning

# core enhancement 13: strategy selector tuning

# core enhancement 16: strategy selector tuning

# core enhancement 19: strategy selector tuning

# core enhancement 22: strategy selector tuning

# core enhancement 25: strategy selector tuning

# core enhancement 28: strategy selector tuning

# core enhancement 31: strategy selector tuning

# core enhancement 34: strategy selector tuning

# core enhancement 37: strategy selector tuning

# core enhancement 40: strategy selector tuning

# core enhancement 43: strategy selector tuning

# core enhancement 46: strategy selector tuning

# core enhancement 49: strategy selector tuning

# core enhancement 52: strategy selector tuning

# core enhancement 55: strategy selector tuning

# core enhancement 58: strategy selector tuning

# core enhancement 61: strategy selector tuning

# core enhancement 64: strategy selector tuning



# Auto-added utility 1
def compute_apr_boost_1(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #1.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 2
def compute_apr_boost_2(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #2.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 4
def compute_apr_boost_4(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #4.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 5
def compute_apr_boost_5(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #5.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 7
def compute_apr_boost_7(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #7.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 11
def compute_apr_boost_11(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #11.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 15
def compute_apr_boost_15(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #15.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 18
def compute_apr_boost_18(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #18.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 19
def compute_apr_boost_19(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #19.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 20
def compute_apr_boost_20(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #20.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 21
def compute_apr_boost_21(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #21.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 28
def compute_apr_boost_28(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #28.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 29
def compute_apr_boost_29(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #29.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 36
def compute_apr_boost_36(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #36.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 44
def compute_apr_boost_44(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #44.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 46
def compute_apr_boost_46(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #46.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 53
def compute_apr_boost_53(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #53.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 69
def compute_apr_boost_69(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #69.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 70
def compute_apr_boost_70(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #70.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 71
def compute_apr_boost_71(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #71.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 73
def compute_apr_boost_73(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #73.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 74
def compute_apr_boost_74(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #74.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 76
def compute_apr_boost_76(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #76.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 77
def compute_apr_boost_77(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #77.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 78
def compute_apr_boost_78(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #78.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 81
def compute_apr_boost_81(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #81.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 82
def compute_apr_boost_82(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #82.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 83
def compute_apr_boost_83(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #83.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 92
def compute_apr_boost_92(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #92.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 93
def compute_apr_boost_93(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #93.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 94
def compute_apr_boost_94(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #94.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 95
def compute_apr_boost_95(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #95.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 96
def compute_apr_boost_96(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #96.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 97
def compute_apr_boost_97(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #97.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 102
def compute_apr_boost_102(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #102.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 104
def compute_apr_boost_104(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #104.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 107
def compute_apr_boost_107(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #107.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 108
def compute_apr_boost_108(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #108.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 109
def compute_apr_boost_109(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #109.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 110
def compute_apr_boost_110(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #110.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 111
def compute_apr_boost_111(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #111.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 113
def compute_apr_boost_113(apy: float, boost: float) -> float:
    """Return boosted APR given APY approximation.
    This is a simple staged automation change #113.
    """
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor



# Auto-added utility 1
def compute_apr_boost_1(apy: float, boost: float) -> float:
    if apy < 0: return 0.0
    factor = 1.0 + max(0.0, boost)
    return apy * factor
